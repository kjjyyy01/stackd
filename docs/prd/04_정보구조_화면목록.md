---
doc_id: PRD-04
title: 정보구조·화면 목록
version: 2.0.0
status: Draft
owner: Jongyeon
last_updated: 2026-08-17
---

# 04 정보구조·화면 목록 (화면 ID·라우트·메타데이터 SSOT)

## 사이트맵·라우트

| SCR-ID | page_code | 화면 | 라우트 | 인증 | 컷 판정 (Day 11) |
|---|---|---|---|---|---|
| SCR-001 | HOME | 홈 (랜딩 + 워크플로우 빌더) | `/` (수정 모드 `/?edit={id}`) | 없음 (수정 모드는 소유자) | 불가 (진입점) |
| ~~SCR-002~~ | — | ~~붙여넣기 파싱~~ | — | — | **결번** (2026-08-16 컷) |
| SCR-003 | CARD | 카드 미리보기·저장 | `/card` | 없음 → **저장 시 로그인** | 불가 (본체) |
| SCR-004 | WF | 워크플로우 상세 | `/workflows/{id}` | 없음 (비공개·hidden은 소유자) | 불가 (루프 착지점) |
| SCR-005 | LEGAL | 법적 문서 | `/privacy` · `/terms` | 없음 | 불가 (법적) |
| SCR-006 | LIB | 라이브러리 | `/workflows` | 없음 | **컷 1순위** |
| SCR-007 | ME | 내 카드 | `/me` | **필수** | 수정 기능만 컷 2순위 |
| SCR-008 | SET | 설정 | `/settings` | **필수** | 불가 (탈퇴 법적) |
| OPS-001 | ADMIN | 운영 — 신고 처리 | `/admin` | **admin만** (env 허용 목록) | 컷 3순위 (웹훅+DB 직접 조치로 대체) |

- SCR 밖 위생·인프라: 404 · favicon · robots.txt · sitemap · 정적 기본 OG 이미지 · `/auth/callback`(OAuth 코드 교환) · 헤더(로고 / 라이브러리 / 로그인 or 내 카드·설정) · 푸터(`/privacy` `/terms` + 문의·피드백 dialog) — Day 4~11 완성
- 서버 표면: `06_API규약_명세.md`

## 페이지별 메타데이터 (Metadata API — 전 페이지 필수, `metadataBase = https://stackd.kr`)

| 라우트 | title | description | OG | canonical | robots |
|---|---|---|---|---|---|
| `/` | Stackd — 내 AI 워크플로우 카드 만들기 | 도구는 아는데 어떻게 쓰는지 모른다면 — 실제 개발자들의 AI 워크플로우를 카드 한 장으로 공유하고 라이브러리에서 예시를 보세요. | 정적 이미지 | `/` | index |
| `/card` | 카드 만들기 \| Stackd | (홈과 동일 톤 1줄) | 정적 | — | **noindex** |
| `/workflows` | 워크플로우 라이브러리 \| Stackd | 개발자들이 공유한 AI 워크플로우 카드 모음 | 정적 | `/workflows` | index |
| `/workflows/{id}` (공개) | `{title} — @{handle} \| Stackd` (동적) | `{situation_short}` + 단계 수 (동적) | **동적** `/api/og?id=` | `/workflows/{id}` | index |
| `/workflows/{id}` (비공개·hidden) | 소유자에게만 렌더 — 타인은 404 | — | 정적 기본 | — | **noindex** |
| `/me` · `/settings` · `/admin` | 내 카드 / 설정 / 운영 \| Stackd | — | 정적 | — | **noindex** |
| `/privacy` · `/terms` | 개인정보처리방침 / 이용약관 \| Stackd | 각 1줄 | 정적 | 각 자기 경로 | index |
| 404 | 페이지를 찾을 수 없어요 \| Stackd | — | — | — | noindex |

- **대표 키워드 "AI 워크플로우 카드"**는 `<title>`·OG·description 담당. h1·부제는 CPY-HOME-001/008 확정 문구 (`00_공통규약.md` §용어)
- h1: 페이지당 1개
- 사이트맵: `/` `/workflows` `/privacy` `/terms` + **공개 워크플로우 상세**(동적 생성, `updated_at` lastmod) — 공개 게시물이 생기므로 v1의 "2개만" 규칙 폐기
- 전 페이지 공통 푸터: `/privacy` `/terms` 링크 + **문의·피드백 dialog** 트리거(CPY-COMMON-001) — 서버 액션 → feedback 저장 + 웹훅 (PRD-06)

## Happy Path (기획서 v2 §핵심 플로우 승계)

`SCR-001` 작성 → `SCR-003` 미리보기(`EVT-CARD-001`) → 저장(미로그인 → GitHub OAuth → 복귀 → `EVT-CARD-002`) → `SCR-004` 상세 → 공유(`EVT-SHARE-001`) → 수신자 `SCR-004` → CTA(`utm_source=card`) → `SCR-001` 재진입 (루프). 병행 진입: `SCR-006` 라이브러리 → `SCR-004`.
