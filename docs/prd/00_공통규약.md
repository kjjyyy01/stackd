# 00 공통규약 (Stackd PRD v2)

> 티어: **Lite** (솔로). 단, 실사용자·계정(GitHub OAuth)·DB(Supabase)·공개 게시물이 있으므로 09_권한·13_써드파티·14_보안·15_분석·17_환경·19_출시기준을 켠다.
> 상위 소스 체인: `docs/day1-decisions.md` → **`docs/day3-mvp-spec.md` (v2, 2026-08-16·17)** → 이 PRD. 충돌 시 상위 문서 승. (`docs/day2-mvp-spec.md`는 v1 기록)
> 전 문서 `status: Approved` — **2026-08-17 사용자 최종 확정** (검토 11항 + 8항 + 최종 점검 5건 반영). Approved만 구현 대상. 이후 수정은 make-prd로만 (status는 그대로, 변경 이력은 git 로그).
> **v2 재작성 (2026-08-17)**: v1 PRD(2026-08-14~16)의 ID를 최대한 유지하되, 폐기된 ID는 결번으로 남기고 재사용하지 않는다.

## ID 매핑

- 기획서 v2의 SCR = 이 PRD의 `SCR-001·003·004·005·006·007·008` (3자리 완전형). **SCR-002(파싱)는 결번**
- 운영 화면 `/admin`은 SCR 밖 — 문서 `OPS-001_admin.md`, page_code `ADMIN` (EL/REQ/TC 접두 `-ADMIN-`)
- page_code: HOME(001) · CARD(003) · **WF**(004 상세, v1의 SHARE에서 변경) · **LEGAL**(005, v1 PRIVACY) · LIB(006) · ME(007) · SET(008) · ADMIN(ops)
- F-/H-/EVT-/BR-/ERR-/CPY-/OQ- 단일 원본은 각 소유 문서. 결번: ~~F-003~~, ~~BR-005~~·~~BR-009~~, ERR-PARSE-*, CPY-PARSE-*, ~~EVT-PARSE-001~~

## SSOT 위임 (이 PRD 밖에 있는 단일 원본)

| 데이터 | 단일 원본 | 비고 |
|---|---|---|
| 범위 외 (Phase 2) | `/backlog.md` | 20_범위외 미생성 — 복제 금지 |
| 디자인 시스템 | `/DESIGN.md` | 12_디자인시스템 미생성 |
| 반응형 브레이크포인트 | Tailwind 내장 (sm/md/lg/xl) | CLAUDE.md 고정 규칙 |
| 모션 규칙 | `/ANIMATION.md` | Day 12~13 몫 |
| 일정·게이트 세부 | `/docs/PLAN.md` §일정 매핑 · Day 11·14·16·17 | 19_출시기준은 요약만 |
| 카탈로그 데이터 | `data/catalog.json` | 스키마는 05 |

## 생략 문서와 사유

03_용어(아래 통합) / 07_API명세(06에 통합) / 12_디자인(DESIGN.md) / 16_테스트전략(PLAN: tdd 도메인 로직 한정 + E2E 1개 + 시각 확인 루프) / 18_관측성(sentry는 PLAN Day 15~16) / 20_범위외(backlog.md) / 22_변경이력(솔로 — git 로그)

## 용어 사전 (03 통합)

| 용어 | 정의 | 금지 표현 |
|---|---|---|
| 워크플로우 | 사용자가 만든 1개의 게시물 = 제목·상황·단계·태그·작성자·공개 여부. DB `workflows` 1행 | "카드"와 혼용 시 아래 정의 따름 |
| 카드 | 워크플로우의 **요약 시각 출력물** (DOM 렌더 + OG/PNG). 상세 설명은 카드에 없음 | "카드 상세" ✗ → "상세 페이지" |
| 상세 페이지 | `/card-detail/{id}` — 카드 + 상황 상세 + 단계별 상세 | — |
| 단계 | 워크플로우 안의 순서 있는 항목 1개 = 도구 + 메모(카드) + 상세(상세 페이지). 2~8개 | "스택"으로 부르지 않음 |
| 도구 | 카탈로그의 개별 항목 또는 직접 입력 항목 (name + category 7종) | — |
| 개발 스택 태그 | 워크플로우의 컨텍스트 태그 0~4개 (language/framework/tool) | 카드의 "STACK" 영역 |
| 라이브러리 | `/workflows` — 모두의 공개 워크플로우 목록 | "갤러리" ✗ (사진 연상, 사용자 결정) |
| 내 카드 | `/me` — 내가 만든 워크플로우 관리 | — |
| 공개 / 비공개 | `is_public` — 비공개는 소유자만 열람, 라이브러리·OG·검색 제외 | "링크만 공유" 모드는 없음 |
| hidden | 모더레이션 플래그 — admin만 토글, 소유자 외 404 | — |
| 초안 | localStorage `stackd:draft` — 저장 전 빌더 상태 | 서버 저장 아님 |
| 작성자 | GitHub 핸들·아바타 스냅샷 (`author_handle`/`author_avatar`) — 사용자 입력 없음 | 닉네임 ✗, 이메일 노출 ✗ |
| 대표 키워드 | **"AI 워크플로우 카드" / "AI workflow card"** — `<title>`·OG·description·런칭 포스트 | h1·부제는 자유(확정 문구는 CPY-HOME-001/008) |

## 버전 용어

| 용어 | 범위 | 기간 |
|---|---|---|
| **v1** | SCR-001·003~008 + `/admin`, Day 20 런칭 범위 | ~2026-09-03(목) 런칭·게시 |
| **v1.x** | 치명 버그·게릴라 피드백 반영만 — **신기능 금지** | 런칭~판정일 9/17 |
| **v2** | 판정 통과 시 backlog 승격분 (라이브러리 필터·Google 로그인·프로필·마크다운 등) | 판정 후 |

## 확정 기술 선정 (2026-08-15 → 2026-08-17 갱신)

| 영역 | 선정 | 비고 |
|---|---|---|
| 백엔드 | **Supabase** — Auth(GitHub OAuth 1개) + Postgres + RLS. `@supabase/ssr`로 서버 컴포넌트·서버 액션 | Storage·Edge Function 미사용. 커스텀 서버 없음 |
| 데이터 접근 | 읽기 = 서버 컴포넌트(anon/RLS) · 쓰기 = **서버 액션**(RLS 하 사용자 세션; feedback·admin만 service role) | 클라이언트에서 직접 쓰기 금지 |
| 짧은 id | `crypto.randomUUID()` 16진 8자 (서버 액션 생성, 충돌 시 재생성) | 의존성 0 (OQ-007 해소) |
| UI 컴포넌트 | **shadcn/ui** (버튼·입력·textarea·dialog·switch·badge·tabs만 add) | 기본 테마 금지 — DESIGN.md 토큰 재스킨 |
| 토스트 | **sonner** | |
| 이미지 | 화면 `<img>` = **next/image** (카드 DOM 렌더는 대상 외) | |
| OG·PNG | OG = next/og `ImageResponse`(가로 1200×630, OQ-012) · PNG 다운로드 = 클라이언트 렌더(OQ-002 — 카드 세로형, OQ-008 해소) | |
| 알림 | Slack 웹훅 (서버 액션 POST) | 실패 무시 |
| **미도입** (v1) | TanStack Query / Zustand / react-hook-form / zod / lz-string / nanoid | 서버 상태는 서버 컴포넌트, 검증은 제한 유틸 순수 함수(BR-007), 폼은 useState |
| 서버 렌더 화면 규칙 | 목록·상세(SCR-004·006·007)는 `loading.tsx`·Suspense 스트리밍 미사용, Error는 서버 컴포넌트 분기(h1 유지 + 재시도 `<a>`), 페이지네이션은 `?page=n` 링크 | JS 비활성 콘텐츠 노출 DoD(PRD-19)와 정합 |
