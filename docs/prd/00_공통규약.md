# 00 공통규약 (Stackd PRD)

> 티어: **Lite** — 솔로, DB·인증·결제 0. 단, 실사용자 런칭이므로 15_분석·19_출시기준은 켠다.
> 상위 소스 체인: `docs/day1-decisions.md` → `docs/day2-mvp-spec.md` → 이 PRD. 충돌 시 상위 문서 승.
> 전 문서 `status: Draft` — 사용자 확정 시 일괄 `Approved` 전환 (Approved만 구현 대상).

## ID 매핑

- Day 2 기획서의 `SCR-01~05` = 이 PRD의 `SCR-001~005` (3자리 완전형)
- F-/H-/EVT-/BR-/ERR-/CPY-/OQ- ID는 이 PRD에서 최초 부여 (단일 원본은 각 소유 문서)

## SSOT 위임 (이 PRD 밖에 있는 단일 원본)

| 데이터 | 단일 원본 | 비고 |
|---|---|---|
| 범위 외 (Phase 2) | `/backlog.md` | 20_범위외 파일 미생성 — 복제 금지 |
| 디자인 시스템 | `/DESIGN.md` | 12_디자인시스템 미생성 |
| 반응형 브레이크포인트 | Tailwind 내장 (sm/md/lg/xl) | CLAUDE.md 고정 규칙 — 커스텀 정의 금지 |
| 모션 규칙 | `/ANIMATION.md` | Day 10~12 몫 |
| 출시 게이트 세부 | `/docs/PLAN.md` Day 9·16·17 | 19_출시기준은 요약만 |

## 생략 문서와 사유

09_권한매트릭스(멀티유저 없음) / 13_써드파티(GA4뿐 — 15에 포함) / 14_보안(회원·결제·PII 저장 없음 — 방어 규칙은 08) / 16_테스트전략(PLAN이 규정: tdd 도메인 로직 한정 + E2E 1개) / 17_환경(env 1개 — 15에 포함) / 18_관측성(sentry는 PLAN Day 15~16) / 22_변경이력(솔로)

## 용어 사전 (03 통합 — 혼동어만)

| 용어 | 정의 | 금지 표현 |
|---|---|---|
| 항목 | 카탈로그의 개별 스택 1개 — 개발 스택(language/framework/tool) 또는 AI agent 스택(skill/plugin/mcp/agent) | — |
| 스택 | 사용자가 담은 항목의 집합 (≤12) | "스택" ≠ 항목 1개 |
| 카탈로그 | `data/catalog.json` 정적 데이터 | — |
| 카드 | 스택+닉네임+템플릿의 시각 출력물 (영어) | — |
| 페이로드 | 카드 상태를 압축 인코딩한 `/stack?s=` 값 | — |
| 대표 키워드 | "AI 스택 카드" / "AI stack card" — 타이틀·h1·OG 일관 사용 | — ("생성기" 금지 규칙은 2026-08-15 취소) |

## 버전 용어 (2026-08-15 확정)

| 용어 | 범위 | 기간 |
|---|---|---|
| **v1** | SCR-001~005, Day 20 런칭 범위 | ~2026-08-31 |
| **v1.x** | 치명 버그 수정·게릴라 피드백 반영만 — **신기능 금지** (판정 오염 방지, PLAN) | 런칭~판정일(런칭+14일) |
| **v2** | 판정 통과 시 backlog 승격분 (갤러리·호스팅 URL·i18n·멀티 도구 등) | 판정 후 |

## 확정 기술 선정 (2026-08-15)

| 영역 | 선정 | 비고 |
|---|---|---|
| UI 컴포넌트 | **shadcn/ui** (필요 컴포넌트만 add) | 기본 테마 그대로 사용 금지 — DESIGN.md 토큰으로 재스킨 (밋밋한 UI 금지 원칙) |
| 토스트 | **sonner** | shadcn 생태계 표준, 경량 |
| 이미지 | 화면에 `<img>`로 그려지는 모든 이미지 = **next/image** | v1 이미지는 전부 직접 제작이라 사실상 전부 해당. 카드 미리보기·공유 카드는 DOM 렌더(이미지 태그 아님)라 대상 외 |
| 인코딩 | lz-string | PRD-05 |
| **미도입** (v1) | TanStack Query / Zustand / react-hook-form / **zod** | 서버 상태 0·전역 상태 1개·폼 1~2필드 — 검증은 제한 유틸 순수 함수 (BR-007) |
