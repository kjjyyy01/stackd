# 킬 크라이테리아 판정 시트 — 2026-09-24(목)

> 판정일 = 런칭(2026-09-10) + 14일. 기준 원문은 `docs/day1-decisions.md` §①, 이벤트 정의는 `docs/prd/15_분석및추적.md`.
> **이 시트의 목적**: 판정일에 숫자만 채우면 결론이 나오게 해서, 관성으로 계속하는 선택지를 없앤다.

## 측정 조건 (판정 전 고정)

| 항목 | 값 |
|---|---|
| 측정 기간 | 2026-09-10 ~ 2026-09-24 (14일, GA4 기간 설정을 이 날짜로) |
| 측정 도구 | GA4 (`stackd.kr` 속성) |
| 사용자 단위 | GA4 `총 사용자` — 세션·이벤트 수가 아니다 |
| 봇·본인 트래픽 | 본인 테스트 세션은 판정에서 제외 — 순회 날짜를 기록해 두고 대조 |

## 판정 기준 3종

| # | 기준 | 임계값 | 측정 방법 | 실측 | 판정 |
|---|---|---|---|---|---|
| ① | 생성 전환율 | **15% 미만이면 피벗·중단** | `card_create` 발화 사용자 ÷ 총 사용자 | | |
| ② | 확산 신호 | **30명 미만이면 확산 가설 기각** | `card_share` 발화 사용자 수 | | |
| ③ | 최소 표본 | **300명 미만이면 판정 보류** | 총 사용자 | | |

**③이 먼저다.** 표본 300명 미만이면 ①②를 계산하지 않고 판정을 보류한다 — 표본 없는 전환율은 노이즈다. 보류 시 조치는 고정: **채널 1회 추가 게시 후 1주 연장**(Threads 재게시 소재는 `docs/launch-posts.md` §3번 글 및 예비 소재 목록).

## 보조 지표 (판정 기준 아님 — 원인 해석용)

| 지표 | 측정 | 실측 | 읽는 법 |
|---|---|---|---|
| `card_preview` → `card_create` 낙차 | 두 이벤트 사용자 수 비 | | 낙차가 크면 **로그인 벽의 비용**. ①이 미달인데 이 값이 크면 제품 실패가 아니라 게이트 위치 문제 |
| `step_add` 발화 사용자 | 사용자 수 | | 단계를 담지도 않았는가(가치 전달 실패) vs 담고도 저장을 안 했는가(빌더 마찰)를 가른다 |
| `card_reshare` | 사용자 수 | | 타인이 남의 카드를 퍼뜨린 증거. 라이브러리·상세의 확산 기여 |
| `login` | 사용자 수 | | 로그인 퍼널 통과량 |
| `utm_source` 분해 | threads / geeknews / disquiet / card / (direct) | | 어느 채널이 표본을 만들었는가. 카드 경유(`utm_source=card`)는 확산 루프 작동의 직접 증거 |

## 판정 결과

- [ ] **지속** — ①② 모두 통과. 다음 액션: backlog 우선순위를 본인 테스트 마찰 기록 + 위 지표로 재정렬
- [ ] **피벗** — ① 미달이나 보조 지표가 원인을 특정함(예: 낙차 큼 = 로그인 벽). 다음 액션: 원인 1개만 고치고 재측정
- [ ] **중단** — ①② 모두 미달이고 보조 지표에도 신호 없음
- [ ] **보류** — ③ 미달. 채널 1회 추가 게시 후 1주 연장, 판정일 재설정: ____

**판정자**: 본인 / **판정일 실행**: ____ / **근거 스크린샷**: ____

> 이 판정 없이 관성으로 계속하는 것이 가장 비싼 실수다 (PLAN.md §판정일 예약).
