# DESIGN.md — 디자인 시스템

> 서비스가 확정되기 전에는 고정 규칙만 기록한다. 미정 값은 임의로 채우지 않는다.
> **토큰 섹션 확정: 2026-08-18 (Day 4 세션 2)** — 구현체는 `app/globals.css`. 이 문서가 SSOT, CSS는 그 사본이다.

## 디자인 판정 (2026-08-18)

- **읽기**: 한국 개발자용 devtool 랜딩+빌더. 페이지의 단 하나의 일 = "내 워크플로우를 담아 카드로 만들게 한다".
- **주제어의 어휘**: agentic 워크플로우 = **순서 있는 도구 호출의 나열**. 그래서 번호·단계 레일·기계 식별자(mono)가 이 제품의 고유 언어다. 장식이 아니라 정보다.
- **테마**: **라이트 단일** (2026-08-18 사용자 결정). 다크는 `backlog.md` 행 — 토큰이 CSS 변수라 나중에 `.dark` 블록 1개로 붙는다.
- **모션 강도 3/10** — PLAN Day 4~11은 애니메이션 금지 구간. `:hover`·`:active`·`:focus-visible` 상태 전이만 허용, GSAP·스크롤 트리거 금지.

## 고정 규칙 (서비스 무관)

- **브레이크포인트**: Tailwind 내장값(sm/md/lg/xl) 그대로 사용 — 별도 정의 금지. JS에서 필요 시 Tailwind config의 screens 참조
- **폰트 로딩**: `next/font` 사용 — FOUT/CLS 방지, LCP 예산(2.5초) 직결
- **정렬 축**: 모든 섹션·컴포넌트의 시작 축이 맞아야 한다. 이 문서의 그리드/컨테이너 규칙 위반은 버그로 취급

## 카드 디자인 레퍼런스 (Day 3 — 2026-08-15 피드백 반영)

> 카드 디자인 작업 시 **참고용** — 그대로 복제하지 않는다. Stackd 카드는 자기 정체성을 가져야 한다.
> 컨셉 키워드 (2026-08-17 v2): **AI 워크플로우 카드** — "내 워크플로우를 동료에게 권한다" (PRD-01). 명함은 **톤만** 차용(여백·타이포·고급감) — 사용 장면·비율·구성요소는 명함이 아님. 카드 = 요약(상황 라벨·제목·@핸들·WORKFLOW 단계 목록·STACK 태그), 상세는 별도 페이지. 카드는 **테마 비의존 고정 팔레트**(PNG·OG로 나가므로), **세로형 560×700(4:5)**(2026-08-17 세로형 결정 → 2026-08-23 수치 확정, OQ-006), OG 1200×630은 별도 가로 구성(OQ-012)

| 레퍼런스 | 참고 포인트 |
|---|---|
| daily.dev Dev Card | 개발자 개인화 카드의 원형 — 정보 밀도·공유 욕구를 만드는 구조 |
| Spotify Wrapped / Receiptify | 바이럴 카드 문법 — 강한 컬러 블록, 리스트의 시각화, "공유하고 싶어지는" 톤 |

- ~~github-readme-stats · GitHub Unwrapped~~ — 2026-08-15 피드백으로 제외
- Day 4~9 시안 작업 시작 시 위 목록에서 2개를 실제로 열어보고 구체 차용 포인트를 이 문서에 추가 기록할 것

## 시그니처 — 단계 레일 (step rail)

이 서비스가 기억될 단 하나의 조형. **순서 있는 단계는 좌측 세로 레일 + mono 번호 노드**로 그린다. 카드 요약·상세 페이지·빌더가 같은 조형을 공유한다.

- 번호(`01` `02` …)는 **실제 순서 정보가 있을 때만** 쓴다. 순서 없는 목록(STACK 태그 등)에 번호를 붙이지 않는다.
- 대담함은 여기 한 곳에만 쓴다. 나머지 화면은 조용하게 — 그라디언트·글로우·장식 도형 금지.

## 카드 조판 (2026-08-23 확정 — OQ-006·OQ-010)

카드는 페이지가 아니라 **고정 캔버스**다. PNG로 나가므로 뷰어 환경에 따라 재배치되면 안 된다.

- **560 × 700px (4:5) 고정.** 폭도 높이도 유동이 아니다. 화면에서 좁아지면 `transform: scale()`로 축소하고, 내부 조판은 건드리지 않는다.
- **유동 폭 금지 (실측 근거)**: 폭을 줄이면 박스 높이는 비율대로 줄지만 내부 텍스트 높이는 거의 그대로라, 320px에서 콘텐츠가 333px 넘친다.
- **단계 메모는 1줄 클램프**(`line-clamp-1`). 상한 60자 × 8단계를 전문 노출하면 자연 높이가 830px(2:3)로 밀려 4:5를 130px·3:4를 83px 넘긴다. 클램프하면 콘텐츠가 698px로 내려와 4:5(700px)에 수렴한다 — **클램프는 비율 성립의 조건**이지 미관 선택이 아니다. 카드가 요약이 아니라 텍스트 벽이 되는 것도 막는다. 잘린 말줄임표가 곧 "상세가 더 있다"는 신호다(OQ-010).
- 입력 상한 자체(1~60자)는 여기가 아니라 `PRD-08 BR-013`이 소유한다. 이 문서는 **표시 규칙**만 정한다.
- **하단 유도 문구**("단계별 설명 보기 →", CPY-CARD-016)는 워터마크와 같은 줄 반대편. 한국어이므로 sans — mono 금지. 조판 안 정적 텍스트라 **PNG에 포함**되며, 상세 페이지(SCR-004)에서만 숨긴다.
- 카드 안 mono는 영문 기계 식별자에만: `@handle`·`WORKFLOW`/`STACK` 라벨·단계 번호·도구명·태그명·`stackd.kr`.

## 홈 캐러셀 조판 (2026-08-25 확정 — Day 8 이식, 동결 시안 승계)

홈 히어로는 풀뷰포트 캐러셀 3장 — 배경 자체가 장면이다(자동 재생 없음, CSS scroll-snap).

- **높이**: `calc(100dvh - 3.5rem)`(헤더 `h-14` 제외), 모바일은 `svh` 병기. 슬라이드 최소 높이 34rem(md+ 38rem).
- **슬라이드 내부 콘텐츠 폭 72rem** — "컨테이너 단일 1024px" 규칙의 **유일한 예외**(풀뷰포트 장면이라 본문 폭보다 넓게, 좌측 축 규칙은 유지).
- **슬라이드 1 h1 스케일(히어로 한 단계 상향)**: 2.75rem / sm 3.5rem / **md 2.25rem(2열 진입 — 열 폭이 절반이라 한 단계 축소, 8/25 실측)** / lg 3.75rem / xl 4.5rem. 부제 1.125rem/sm 1.25rem/md 1.125rem/lg 1.25rem. 질문(q)·답 타이포도 같은 원칙 — **md 2열 진입 시 한 단계 축소, xl에서 원 크기 복귀**.
- **카드(560×700) 재사용 배율**: 슬라이드 1 카톡 슬롯 `0.26`/sm `0.38` · 슬라이드 3 요약 슬롯 `0.36`/sm `0.52`/md `0.42`/lg `0.52` · 과정 레일 2단계 `0.5` · 쇼케이스 **모바일·md `0.58` 블리드 없음**(모바일 중앙 — 360px 폰까지 안 잘림 / md 우측 정렬 — 2열 열 폭 336px에 수렴)/sm `0.9`(−12vw, 1열이라 여유)/**lg `0.8`(블리드 없음 — 콘텐츠 폭 안 수렴, 뷰포트 우측 32px 여백. 가장자리 밀착은 답답하다는 사용자 판정)**/xl `1.0`(−10vw — 1280부터 뷰포트 여백이 뻗침을 흡수, 8/25 실측). **원칙: 어느 구간에서도 카드 내용이 잘리지 않고, 가장자리에 밀착하지도 않는다** — 블리드는 여백이 뻗침을 흡수하고도 남는 폭(xl+)과 1열(sm)에서만. 축소 슬롯은 `overflow-hidden` 필수 — scale은 페인트만 축소하고 레이아웃 560px이 그리드 최소폭을 민다(8/25 실측).
- **내비**: 화살표 3.25rem(52px) 원형·**xl+만 표시**(lg 이하는 콘텐츠 72rem이 뷰포트를 채워 본문과 겹침 — 터치 스와이프+카운터로 충분, 8/25 실측), 카운터 mono `n / 3` 우하단, FAB 3rem(48px) — 화살표와 같은 시각 어휘(중립 원형·헤어라인 테두리). viz 화살표(→)는 wrap 구간(lg 미만)에서 자체 줄 + 90° 회전(↓).
- **장면 지면 토큰(신규 2종)**: `--chat-bg: #bacee0`(슬라이드 1 메신저 지면) · `--chat-me: #fee500`(내 말풍선). 슬라이드 2 `--bg` · 슬라이드 3 `--muted` 재사용. 카톡 로고는 쓰지 않는다 — 색과 조판만으로 장면을 암시.
- `prefers-reduced-motion: reduce` 시 스크롤 behavior `auto`.

## 그리드 / 컨테이너 폭

- **페이지 컨테이너는 단 하나**: `mx-auto w-full max-w-5xl px-4 sm:px-6 lg:px-8` (최대 1024px). 헤더·본문·푸터 전부 동일 클래스 → 좌측 축이 자동으로 맞는다. *(예외 1곳: 홈 캐러셀 슬라이드 내부 72rem — §홈 캐러셀 조판)*
- **읽기 폭**: 산문은 `max-w-[62ch]`. 한글은 라틴보다 글자당 폭이 커서 62자 근처가 한 줄 상한.
- **섹션 세로 여백**: `py-16 sm:py-24`. 헤더 높이 `h-14`(56px) 고정.
- 다단은 CSS Grid만 (`grid grid-cols-1 md:grid-cols-N`). flex 퍼센트 계산(`w-[calc(33%-1rem)]`) 금지.

## 정렬 축(시작 지점) 규칙

- **좌측 정렬이 기본. 본문·헤딩의 중앙 정렬 금지.** 컨테이너 안쪽 좌측 축에서 모든 섹션이 시작한다.
- 중앙 정렬 허용 예외 3곳뿐: 빈 상태(Empty), 404, 토스트·다이얼로그 내부.
- 아이콘·번호·텍스트가 한 줄에 있으면 **베이스라인이 아니라 좌측 축**을 맞춘다 (레일 번호 ↔ 단계 제목).

## 스페이싱 스케일

- **4px 배수만.** 허용 토큰: `1 2 3 4 6 8 10 12 16 20 24` (Tailwind 기본 스케일).
- `p-[13px]`·`mt-[7px]` 같은 임의값 **금지** — 발견 즉시 버그.
- 관계의 근접성 = 간격: 같은 덩어리 안 `gap-2`~`gap-3`, 덩어리 사이 `gap-6`~`gap-8`, 섹션 사이 `py-16`+.

## 모서리 반경 (shape lock)

- `--radius: 0.5rem`(8px) 하나로 고정. 버튼·인풋·카드·다이얼로그 전부 이 스케일(`rounded-lg`/`rounded-xl`)에서 파생.
- **`rounded-full`은 태그·배지·아바타에만.** 그 외 임의 radius 금지.

## 타이포 스케일 + 폰트

- **본문·제목**: `IBM Plex Sans KR` (`next/font/google`, 400/500/600/700) — 한글을 위해 별도로 그려진 휴머니스트 그로테스크. 한국 devtool 기본값(Pretendard)을 의도적으로 피한 선택.
- **기계 식별자**: `IBM Plex Mono` (400/500) — 도구명·단계 번호·STACK 태그·코드처럼 **기계가 읽는 문자열에만**. 한국어 산문에 mono 금지.
- 두 가족은 같은 설계(IBM Plex)라 메트릭이 맞는다. 한글 가족 2개 혼용은 금지 — 메트릭이 어긋난다.

| 역할 | 클래스 | 행간 | 자간 | 굵기 |
|---|---|---|---|---|
| display (h1) | `text-4xl sm:text-5xl lg:text-6xl` | `leading-[1.15]` | `tracking-[-0.025em]` | 600 |
| h2 | `text-2xl sm:text-3xl` | `leading-[1.25]` | `tracking-[-0.015em]` | 600 |
| h3 | `text-lg sm:text-xl` | `leading-[1.4]` | `tracking-[-0.01em]` | 600 |
| body | `text-base` | `leading-[1.75]` | 0 | 400 |
| small·메타 | `text-sm` | `leading-[1.6]` | 0 | 400 |
| 라벨(mono) | `text-xs font-mono uppercase` | `leading-none` | `tracking-[0.1em]` | 500 |

**한글 조판 규칙 (라틴과 다름)**

- **`word-break: keep-all` 필수.** 없으면 어절 한가운데("어떻 / 게")서 잘린다. `overflow-wrap: break-word`와 짝으로 — URL 같은 긴 문자열만 강제로 끊는다. `body`에 전역 적용됨.
- **본문 행간 1.75.** 한글은 x-height가 크고 글자 폭이 균일해 라틴 기준 1.5로는 답답하다.
- **본문 자간 0.** 한글에 음수 자간을 주면 낱자가 뭉친다. 음수 자간은 **28px 이상 제목에만** (그 크기에선 한글도 벌어져 보인다).
- **mono는 한글에 쓰지 않는다.** IBM Plex Mono에 한글 글리프가 없어 대체 폰트로 떨어지고 자간이 어긋난다. 라벨이 한국어면 sans, mono 라벨은 영문(`WORKFLOW`·`STACK`)만.
- 폰트 크기는 `rem`으로 — 사용자 브라우저 글자 크기 설정을 따라간다.

**폰트 로딩 트레이드오프(기록)**: Google Fonts의 IBM Plex Sans KR은 한글이 `unicode-range` 슬라이스 ~94개로 쪼개져 오고, `next/font`는 이름 있는 subset(`latin`)만 preload한다 → **한글 슬라이스는 preload 안 됨**. `display: swap` + `adjustFontFallback`(size-adjust 대체폰트 자동 생성)으로 **CLS는 막고 LCP는 오히려 유리**(대체 폰트로 즉시 그림), 대신 한글에 짧은 FOUT이 있다. 이걸 없애려면 Pretendard 서브셋 자체 호스팅(`next/font/local`)이 필요 — v1은 하지 않는다.

## 컬러 토큰

**라이트 단일.** 값은 oklch, 괄호 안은 sRGB 근사. 대비비는 실측값(WCAG 2.1).

| 토큰 | oklch | hex | 용도 |
|---|---|---|---|
| `--background` | `0.985 0.002 265` | `#f9fafb` | 페이지 바탕 — 순백 아님(차갑게 아주 살짝) |
| `--card` / `--popover` | `1 0 0` | `#ffffff` | 올라온 표면 — 배경보다 **밝아서** 테두리 없이도 떠 보인다 |
| `--foreground` | `0.19 0.012 265` | `#111419` | 본문 잉크 — 순검정 아님(청색 편향) |
| `--muted` | `0.965 0.004 265` | `#f2f3f6` | 옅은 면(코드 블록·비활성 행) |
| `--muted-foreground` | `0.52 0.015 265` | `#656972` | 보조 텍스트 |
| `--border` | `0.912 0.006 265` | `#e0e2e6` | 헤어라인(구분용, 비필수 정보) |
| `--input` | `0.64 0.014 265` | `#888c95` | **입력 컨트롤 경계** — 배경 대비 3.22:1 (WCAG 1.4.11) |
| `--primary` | `0.52 0.19 262` | `#245fd4` | 유일한 액센트 |
| `--primary-foreground` | `1 0 0` | `#ffffff` | 액센트 위 글자 |
| `--ring` | `= --primary` | | 포커스 링 |
| `--destructive` | `0.52 0.19 25` | `#be222a` | 파괴적 행위·에러만 |

**대비 실측** — foreground/background 17.7:1 · muted-foreground/background 5.28:1 · primary 위 흰 글씨 5.71:1 · primary/background 5.47:1 · input 경계/background 3.22:1 · destructive/background 5.82:1. 전부 WCAG AA 통과.

**색 사용 규칙 (이게 진짜 규칙이다)**

- **액센트는 하나, 그리고 "행동·상태"에만.** 기본 버튼, 링크, 포커스 링, 현재 단계 — 여기까지. **장식으로 쓰지 않는다.**
- **그라디언트·글로우·네온 금지.** 표면 구분은 배경 밝기 차(`--background` vs `--card`)로, 그림자가 필요하면 배경 색상에 물들인 낮은 불투명도만.
- **순검정(`#000`)·순백 텍스트 금지.** 깊이가 죽는다.
- 카드(PNG·OG로 나가는 산출물)는 이 팔레트와 **무관한 고정 팔레트**를 따로 갖는다 — 뷰어 테마에 의존하면 안 되므로.

### 카드 고정 팔레트 (2026-08-27 확정 — EL-CARD-010)

카드는 PNG·OG로 나가므로 CSS 변수를 못 쓴다. `components/workflow-card.tsx`가 이 표의 사본이다.

| 역할 | hex | 비고 |
|---|---|---|
| 지면 | `#ffffff` | 카드 바탕 — `--card`와 같은 값이지만 독립 상수 |
| 잉크 | `#111419` | 제목·본문 |
| 보조 | `#656972` | 단계 메모·핸들 |
| 헤어라인 | `#e0e2e6` | 단계 레일 |

**액센트 3종** — 사용자가 카드마다 고른다(EL-CARD-010 스와치)

| 슬러그 | oklch | hex | 흰색 대비 |
|---|---|---|---|
| `ink` (기본) | `0.19 0.012 265` | `#111419` | 18.5:1 |
| `navy` | `0.50 0.13 255` | `#2863ab` | 6.0:1 |
| `forest` | `0.50 0.12 155` | `#0b7643` | 5.7:1 |

- **명도 L을 0.50으로 고정**하고 색상만 바꾼다 — 더 밝으면 흰 배경 위 텍스트가 안 읽히고, 더 어두우면 ink와 구분이 안 된다. 고정하면 세 색이 시각적으로 같은 무게를 갖는다
- **흰색 대비 4.5:1이 하한**이다. 액센트는 단계 번호 뱃지(배경, 흰 글씨)와 "단계별 설명 보기 →"(텍스트, 흰 배경) 양쪽에 쓰이는데, 대비비는 방향과 무관해 한 번만 만족시키면 된다
- 팔레트를 늘릴 때도 같은 조건 — 위 두 줄이 유일한 제약이다

## 접근성 기준선 (협상 대상 아님)

- 포커스는 항상 보인다 — `focus-visible:ring-3 ring-ring/50`, 테두리만으로 대체 금지.
- 입력에 label 필수. placeholder를 label로 쓰지 않는다.
- `prefers-reduced-motion: reduce`에서 상태 전이 duration 0으로.
- 모든 이미지 alt, 페이지당 h1 1개.
- 터치 타깃: 헤더·푸터 크롬은 최소 36px(WCAG 2.5.8 AA 24px 상회), **화면 본문의 주요 CTA는 44px**.
