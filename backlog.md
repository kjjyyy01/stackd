# backlog.md — 스코프 방어

> 3주 계획(docs/PLAN.md) 밖의 모든 아이디어는 여기로 보낸다. Day 2 이후 기능 추가 아이디어도 전부 여기로.

## 기능

- 호스팅형 카드 URL (짧은 링크·조회수 — 백엔드 필요, Day 2 판정으로 컷)
- 갤러리·탐색 (v2 플랫폼 성장 방향)
- i18n — 카드 다국어 출력
- codex·gemini 등 멀티 도구 프리셋
- AI(LLM) 파싱 — 정규식 파싱 인식률이 실사용에서 문제로 확인될 때만
- **붙여넣기 파싱 전체 (v1 SCR-002 — settings.json/CLAUDE.md 2형식)** — 2026-08-16 컷: 워크플로우(순서·상황)엔 파싱이 기여 못 함
- `package.json` 붙여넣기 파싱 (개발 스택 자동 추출)
- QR로 건네기 — 2026-08-16 컷 (대면 장면 제거, 명함은 카드 톤에만)
- 갤러리 검색·필터·정렬·좋아요·댓글 — 카드 30장 넘으면 필터부터 (2026-08-16)
- 상세 페이지 마크다운 렌더 — v1은 일반 텍스트+줄바꿈
- 다른 OAuth(Google)·이메일 로그인 — v1은 GitHub만
- 프로필 페이지(닉네임별 카드 모음)
- "이 워크플로우 따라 담기"(포크) — 기능으로 제공하지 않음(사용자 결정 2026-08-16), 재검토 시 소유권·출처 표기 전제
- 개발 스택 카테고리 세분화(DB·클라우드·모바일 등) — v1은 language/framework/tool 3종
- 유저 직접 입력분 카탈로그 후보 수집 파이프라인 (v2 성장 구조)
- 명함/공유 카드 **타입 선택 모드** — 판정일 사용 패턴(1:1 전달 vs 커뮤니티 게시) 데이터로 결정 (Day 3 논의)
- 카드 템플릿 다종화 — v1은 시그니처 1종 + 액센트 컬러 고정 (Day 3 확정)
- 명함 구성요소 기능화(연락처·연락처 QR·명함첩) — PRD-01 가드로 v1 금지, 호스팅형 URL(DB) 전제. ※ 공유 링크 QR은 v1 포함(Day 3 확정)
- 다른 기기에서 내 카드 수정 — v1은 만든 브라우저(localStorage)에서만 가능. 계정·호스팅형 URL 전제 (2026-08-16). ※ 타인 카드 포크는 기능으로 제공하지 않음(사용자 결정)
- 앱/PWA 전환 — 명함첩·재방문 기능과 세트, v2 판정 후 (Day 3 논의: 웹 확정)
- **도구 자동 링크 — 상세 페이지 × 카탈로그 URL 조인** (2026-08-24): `steps[].tool.name`을 `catalog.json` 이름 매칭 → 매칭 시 도구명을 출처 링크(공식 사이트·GitHub)로 렌더. 스키마·빌더 UI·입력 변경 0 (카탈로그에 URL 기수집, 직접 입력분만 링크 없음). **Day 11(8/28) 스코프 컷 판정 때 SCR-004 AC 1줄로 흡수할지 결정**
- 단계별 링크 직접 첨부 (2026-08-24): 작성자가 단계에 GitHub·홈페이지 URL 첨부. 구조 변경 등급 — 스키마(`steps[].tool.url`)·서버 URL 검증·스팸 방어(`nofollow`·신고 연동)·빌더 UI. 런칭 후
- **"에이전트용으로 복사"** (2026-08-24): 상세 페이지에서 워크플로우 전체(상황·단계·도구·상세)를 구조화 마크다운으로 변환해 복사 → 독자의 Claude Code 등 에이전트가 카드를 읽고 세팅 대행. 데이터 모델 변경 0(표현 변환만), "실행 가능한 카드" 차별화 후보. **판정일(9/22) 통과 후 1순위 실험**
- **AX(Agent Experience) 3단 — 에이전트도 사용자로 취급** (2026-08-25, [출처 세션](https://claude.ai/share/82c761cb-c041-4fc0-b0ae-d2ffdc97a86f), 8/25 확장). 0단(SSR 콘텐츠·시맨틱 마크업·Day 17 접근성 패스 — a11y 트리는 컴퓨터 유즈 에이전트의 읽기 표면이기도 함)은 이미 상시 적용 중:
  - ① **Day 17(9/3) 위생 묶음** (사이트맵·JSON-LD 작업에 얹기, 합계 ~1시간): llms.txt(서비스 정의·라우트·공개 API — AI 검색 유입 근거는 약함, 코딩 에이전트용 B2A 표면 목적) + 카드 상세 JSON-LD를 `HowTo` 타입으로(상황=name·단계=step — Google 리치 결과 노출은 2023 중단, 목적은 에이전트 가독성) + robots.txt AI 봇 명시 허용(Anthropic 봇은 ClaudeBot·Claude-SearchBot·Claude-User 3종 분리) + Vercel 방화벽 "Block AI Bots" 룰 Off 확인(기본 Off) + stackd.kr DNS가 Cloudflare 프록시 경유면 AI 크롤러 기본 차단 정책(2026-09-15~) 해당 여부 확인
  - ② **공개 read API** — `GET /api/workflows`(목록)·`/api/workflows/{id}`(상세) JSON, Route Handler 2개. public read RLS 이미 있음, hidden 필터(BR-018)만 주의. + 카드 상세 **`.md` 라우트**("에이전트용으로 복사"와 변환 함수 공유 — Anthropic·Mintlify 문서의 URL+.md 패턴) + **OpenAPI 스펙 1파일**(③ MCP 자동 생성 도구의 입력이 됨) → **런칭 후**
  - ③ **MCP 서버** — search/get 툴 노출로 외부 에이전트가 직접 호출. "에이전트용으로 복사"(수동)의 자동판이라 세트 실험. 확장 후보: 카드 → SKILL.md/슬래시 커맨드 파일 익스포트("실행 가능한 카드"의 완성형). `.well-known/mcp.json` 디스커버리는 드래프트 단계(SEP-1649)라 착수 시점에 스펙 상태 재확인 → **판정일(9/22) 통과 후, ② 전제**
  - 기각(2026-08-25): RSS 피드(② JSON과 소비자 중복) · GEO 콘텐츠 최적화 패스(v1 카드 수로는 효과 측정 불가) · 사이트 내 챗/NLWeb(유지비 대비 무가치) · 에이전트 유입 관측 미들웨어(GA4는 봇을 못 보지만 Vercel 로그로 충분, 코드 0)

## 개선

- **자체 Admin 페이지 + 접수 DB** — v1은 폼 서비스 대시보드가 Admin 역할 (자체 구축은 DB·인증이 생기는 순간 = 백엔드 판정 뒤집기라 v2로. 갤러리·차단 리스트가 생기면 그때 진짜 필요해짐)
- 홈 진입 프리로더 연출 — 판정 보류 (LCP 예산·서버 렌더 위생과 충돌. 대안: 히어로 엔트런스 스태거 모션은 Day 10~12 범위 내)
- 직접 입력 금칙어 필터 — 악용 실사례 확인 시에만 (v1은 구조적 방어로 충분 — PRD-08)
- **신고·문의 답장** — 운영자가 쓴 답장을 신고자에게 전달. **2026-08-27 보류 판정**(본인) — 지금 상태 유지. 설계는 나와 있다: `feedback`에 `reply`·`replied_at` 컬럼 + admin 입력란 + 내 내역 화면에 표시(SCR-008 있으면 증분 45분). 제약 2가지 — ①익명 신고은 `reporter_id`가 null이라 전달 불가(로그인 신고자만) ②이메일 발송은 PII 수집이라 PRD-14 개정 동반. 피신고자 쪽 통지는 **이미 구현됨**(카드 상세·`/me`에 hidden 사유 + 이의제기 링크)
- **소유자 CTA의 UTM 계수 오염** — SCR-004 EL-WF-007이 소유자에게도 `utm_source=card&utm_medium=share`로 나간다. PRD-15:45가 이 UTM을 "루프 작동 직접 증거(H 보조)"로 쓰므로 **본인 클릭이 공유 유입에 섞인다**. v1은 유지 판정(2026-08-27) — 판정일에 이 수치를 읽을 때 소유자 클릭분을 감안할 것. 고치려면 소유자에겐 문구 교체 + UTM 제거

## 비즈니스·수익화 (판정일 전 착수 금지 — 판정 오염 방지)

- 단계 0 (지금~판정일): 수익화 0 — 비로그인·무료 유지, 킬 크라이테리아 측정 순수성 우선
- 단계 1 (판정 통과 후): 프로 템플릿·커스텀 브랜딩(워터마크 제거 포함) 소액 결제 — 호스팅형 카드 URL과 세트
- 단계 2 (갤러리 v2): 도구 벤더 스폰서 배치(카탈로그 내 프로모션 슬롯), 어필리에이트 출처 링크
- 단계 3 (장기): 팀/채용용 스택 프로필 — "이 회사 개발팀의 AI 스택" (B2B 확장)

## 컷된 화면

## 발동 조건부 항목 (브라우저 확장 등)

> 감으로 "나중에"가 아니라 **수치·사건 트리거**로 적는다. 트리거가 안 켜지면 착수하지 않는다.
> 아래 RAG/Agent 4항목은 2026-08-26 조사 결과 (후보 8개 → 3중 크로스체크: 제약 정합성 / 중복·사다리 / 실현성·실패모드).

- **A6-① 카드 → 실행 자산 익스포트 (결정론, LLM 0)** (2026-08-26): 카드를 `SKILL.md`·슬래시 커맨드·MCP 설정 파일로 변환. AX ③의 "카드 → SKILL.md 익스포트" 씨앗의 실체화. 순수 문자열 변환이라 **"에이전트용으로 복사"·AX ② `.md` 라우트·llms.txt가 변환 함수 1개를 공유**한다(3기능 공용 기반 — 하나를 지으면 나머지 절반이 딸려온다). **발동: 판정일(9/22) 통과 — 기존 "복사" 1순위와 동시 착수**
- **A6-② 익스포트 에이전트 층** (2026-08-26): 각 단계 도구의 실제 설치 명령·설정 스키마를 조사해 익스포트에 덧붙임 (탐색 필요 = 진짜 에이전트). **발동: A6-①의 실사용 흔적 확인 후** — ①만으로 80%가 나오므로 수요 확인 전 착수 금지
- **A1 워크플로우 진단·리프레시 에이전트** (2026-08-26): 내 카드를 읽고 각 단계 도구의 현재 상태를 웹서치로 조사 → "이 MCP는 폐기됨 / 이 단계는 X로 대체 가능" 제안. 사전 명세 불가 + 다단계 + 탐색 = **v1 범위에서 에이전트가 본질적으로 필요한 유일 후보**. 생성이 아니라 진단이라 킬 크라이테리아(카드 생성 동기)를 오염시키지 않는다. **발동: 판정 통과 AND 재편집·재방문이 유의미**
  - 착수 시 PRD-13 4칸 필수 — 실패: 진단 섹션만 미노출(카드·상세는 유지, ERR-OG-001 폴백과 동형) / 타임아웃: Vercel Hobby+Fluid **300초** 하드리밋 아래 자체 상한 + 부분 결과 저장, 스트리밍 필수 / 어뷰징: 로그인 필수 + 카드당·일당 실행 상한(RLS 소유자 판정 재사용) / 비용: 카드 수 × 상한으로 **유계**
  - **진단 결과는 소유자 비공개 제안 — 공개 상세 자동 반영 금지.** 웹서치 기반 "폐기됨" 판단이 틀리면 타인 도구에 대한 사실주장 오류가 공개 페이지에 실린다
- **RAG 일체 (pgvector·임베딩)** (2026-08-26): **발동 = 공개 카드 ≥ 1,000장.** 근거 — 현 코퍼스는 `catalog.json` name+description 전체가 **20,885 B(≈5~6K 토큰)**로 컨텍스트의 0.5%다. 카드 1장 ≈ 1K 토큰이라 1,000장쯤에서야 전량 주입이 불리해진다. 그 전까진 통째로 넣는 게 더 싸고 정확하다. **RAG의 전제조건은 기능 추가가 아니라 규모이고, 규모는 판정 결과다**
  - 선행 사다리(RAG 검토 전 반드시 소진): 1단 카탈로그 검색 `description` 매칭 — 현재 `name`만 봐서 268건 중 mcp·plugin·skill **153건이 이름만으론 도달 불가**(별건: SCR-001 결함) → 2단 **`pgroonga`**(한국어 형태소 전문검색, 임베딩·벤더 0) / `pg_trgm`. **2단 발동: 1단으로 부족하다는 실사용 신호**
  - 착수 시 재확인 — Supabase `vector` 0.8.2는 **미설치·설치 가능** / **임베딩 벤더가 하나 더 필요**(Supabase 공식 RAG 예제는 전부 OpenAI 임베딩, Anthropic 엔드포인트 목록엔 임베딩 없음 → 착수 시점 재확인) = PRD-13 행·시크릿 추가 / **무료 500MB 벽**: 1536차원 float4 = 6,144 B/행이라 10만 청크 ≈ **614MB로 초과** → 차원 축소·청크 상한 필수
  - 기각(2026-08-26): 도구 체인지로그 아카이브 코퍼스(**A1의 웹서치가 대체** — 수집·신선도 유지를 서버사이드 툴이 대신하므로 우리 인프라·벤더·용량 전부 0) · 툴 위키 268개(저작권·크롤 유지비 + 같은 용량 벽) · 상황→워크플로우 초안 생성(**킬 크라이테리아 오염** — 카드 생성 동기를 측정하는데 LLM이 대신 써주면 무엇을 측정한 건지 알 수 없음) · 자동 모더레이션(악용 실사례 0건, "금칙어 필터" 항목과 같은 자리)
